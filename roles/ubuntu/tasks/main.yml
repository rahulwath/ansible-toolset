---
- name: Install Podman on Ubuntu
  package:
    name: podman
    state: present
  become: yes
  when: ansible_os_family == "Debian"

- name: Install podman-compose
  ansible.builtin.pip:
    name: podman-compose
    state: present
  become: yes
  when: ansible_os_family == "Debian" 

